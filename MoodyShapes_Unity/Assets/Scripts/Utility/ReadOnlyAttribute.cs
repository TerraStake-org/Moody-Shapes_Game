using UnityEngine;

/// <summary>
/// An attribute that makes a field read-only in the Inspector.
/// Used for displaying debug information without allowing direct editing.
/// </summary>
public class ReadOnlyAttribute : PropertyAttribute
{
    // This is just a marker attribute
}
